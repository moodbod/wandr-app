# Convex

The schema is ready in `schema.ts`.

Run `bunx convex dev` after installing dependencies to generate `convex/_generated`.
Add queries and mutations after generation so they can import the typed helpers from
`./_generated/server`.
